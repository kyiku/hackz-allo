# Runner側のみが保持する秘密情報（タスク1.4）。実値は .env に置き、コミットしない。
# GitHub Personal Access Token（repo / workflow 権限）
GITHUB_PAT=
# Anthropic API Key（Claude Agent SDK / Anthropic SDK）
ANTHROPIC_API_KEY=

# Backend ポート
PORT=3001
