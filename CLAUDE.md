# GitHub Issue RPG

AIによるTDD開発をRPGの戦闘体験にするWebツール。要件定義は `.kiro/specs/github-issue-rpg/` にある。

## Git運用

- **gitは自由に使ってよい。** ブランチ作成・コミット・push・PR作成などを許可なく実行してよい。
- spec / 設計ドキュメント（`.kiro/specs/` 配下の `requirements.md`, `design.md`, `tasks.md` 等）も**コミットしてよい**。
  - 注記: ユーザーグローバルの `~/CLAUDE.md` には「specをコミットしない」方針があるが、本プロジェクトではこのローカルCLAUDE.mdの指示を優先する。
