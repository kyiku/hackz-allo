/**
 * Runner エントリポイント。
 *
 * AIエージェント(Claude Agent SDK)の実行、GitHub連携、issueポーリング等を担う。
 * オーケストレーションの実体はタスク4.x / 9.2 で実装する。
 */

export {}

// eslint-disable-next-line no-console
console.log('[runner] bootstrap placeholder')
